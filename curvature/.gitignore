# Generated by the study and regenerable from the scripts beside this file.
# `ruptures/` is 84 GB of native rupture files, `data/` 402 MB of rasters, and the
# HTML is typst's export plus the assembled artifact page.
ruptures/
data/
*.html
