[package]
name = "genslip"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Kinematic rupture model generation: a port of genslip v5.6.2."

[dev-dependencies]
num-complex = "0.4.6"
proptest = "1"

[lints]
workspace = true

[dependencies]
geographiclib-rs = "0.2.7"
num-complex = "0.4.6"
rand = "0.10.2"
rand_distr = "0.6.0"
rand_pcg = "0.10.2"
rustfft = "6.4.1"
# The error type. Pinned to what the sibling HF port uses and what is in the offline
# registry; `error.rs` says why the crate needs one at all.
thiserror = "2.0.19"
