[package]
name = "rupture-generator-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Python bindings for the rupture generator."
publish = false

[lib]
name = "_core"
crate-type = ["cdylib"]

[dependencies]
genslip.workspace = true
numpy = "0.29"
pyo3 = { version = "0.29", features = ["extension-module"] }

[lints]
workspace = true
